[package]
name         = "smiths-mcp"
edition      = { workspace = true }
rust-version = { workspace = true }
version      = { workspace = true }
license      = { workspace = true }
description  = "Model Context Protocol server (stdio + HTTP/SSE) for LLM agents."

[dependencies]
smiths-core = { workspace = true }

[lints]
workspace = true
